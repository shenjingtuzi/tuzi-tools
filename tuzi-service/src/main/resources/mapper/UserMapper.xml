<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mycenter.mapper.UserMapper">
    <!-- 示例：根据openid查询用户（MyBatis-Plus 已实现，此处仅作演示） -->
    <select id="selectByOpenid" resultType="com.example.mycenter.entity.User">
        SELECT * FROM user WHERE openid = #{openid}
    </select>
</mapper>